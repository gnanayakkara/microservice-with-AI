--How to run postgres in localhost

docker run --name postgres -p 5433:5432 -h 127.0.0.1 -e POSTGRES_USER=root -e POSTGRES_PASSWORD=password -d postgres


--Docker pgadmin
docker run --name pgadmin-tutorial -p 5050:80 -e PGADMIN_DEFAULT_EMAIL=admin@example.com -e PGADMIN_DEFAULT_PASSWORD=password -d dpage/pgadmin4

--Rabbit MQ
# latest RabbitMQ 4.x
docker run -it --rm --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4-management
http://localhost:15672
guest:guest